---
date: 2026-08-28
status: disposable - delete once used
supersedes: 2026-08-28_next_session_prompt.md (that one is spent; delete it)
purpose: resume at the exact point script 07 becomes runnable
updated: 2026-08-28 - script 07 is WRITTEN and NOT RUN; four items need sign-off
---

# Handoff

Everything is committed and pushed. `main` is level with `origin`, working tree
clean.

**Script 07 is written and has not been run.** Section 8 of it is a 20-40 minute
job that writes 0.5-1 GB into `results/mito_null/`; the four decisions it makes
beyond its approved spec are listed under "Sign-off needed" below. Read those
before sourcing it.

## Resume prompt

Paste this into a fresh session started in `/Users/gs/code/myc_human_validation`:

```
Read CLAUDE.md, then docs/2026-08-28_handoff.md. Read the plan
(docs/2026-08-27_human_validation_plan.md) sections 2, 7.2, 8 and 10 only.

Do NOT re-derive or re-audit anything. All gates and decisions are settled and
recorded in dated notes in docs/. The mouse repo is deliberately NOT attached;
read it read-only if needed with
  git -C /Users/gs/G/data/MK_myc_2022/myc_mouse show HEAD:<path>

Scripts 00-06 are written AND RUN, and their outputs are in results/.
Script 07 is WRITTEN and NOT RUN. Read it, then the "Sign-off needed" section of
this handoff. Do not run scripts - I source them in Positron.

Two further decisions block script 09, not 07. Do not start 09.
```

---

## Where the work actually is

**Written, run, validated:** `00`-`06`, `07`, `08`.
**Written, NOT run:** `09_interaction_models.R`.
**Next:** re-source `08` (it gained the limb columns 09 needs), then source `09`.

`09` is built to `docs/2026-08-29_script09_build_spec.md`, signed off before any
coefficient was computed. Two primary fits and no others; ~110 observed fits plus
68,000 null fits, roughly a minute or two.

```
results/
  tcga_brca_vst.rds            LOG scale (VST)   -> GSVA/ssGSEA, kcdf Gaussian
  tcga_brca_linear.rds         LINEAR (DESeq2)   -> mitoPPS ONLY
  tcga_brca_genomics.rds       mutations, purity, leukocyte, aneuploidy, RPPA
  tcga_brca_covariates.rds     the patient x covariate table + GISTIC matrices
  tcga_brca_myc_scores.rds     M-a, M-b, M-c + concordance
  g1_overlap_audit.rds         the estimators, raw and MitoCarta-stripped
  g1_correlation_criterion.rds G1's second criterion
  g2_cnv_cooccurrence.rds      the gate result
```

Current shape: **1,095 patients, 18,115 genes**. Block C complete cases: **938**.

## Sign-off needed before script 07 is sourced

Four things the script decides that its approved spec did not cover. All four
are flagged in the script itself at the point they happen.

1. **The mitoPPS universe excludes the mouse's `Apoptosis-PRO` / `Apoptosis-ANTI`
   pathways** (script section 5.1). Two independent reasons: those lists carry
   `Bbc3` and `Bcl2l1`, whose human orthologs are PRIME's numerator and
   denominator, so they would put the endpoint's genes into the exposure's
   construction; and they are a mouse-curated set, so uppercasing them into
   human is the ortholog projection CLAUDE.md forbids. MitoCarta's own
   human-native `Apoptosis` pathway (35 genes) stays in. Consequence: the human
   and mouse mitoPPS universes differ by two pathways.
2. **GSVA `minSize = 3`, not 10.** Forced by the per-complex sets the plan
   requires - `Complex II` is 4 genes and `ATPase` is 5. Every set below 10
   genes is flagged `small` in each output table. Script 06 used 10 and is
   unaffected.
3. **Null draws are without replacement within a ventile.** The mouse drew with
   replacement, which is harmless for a mean of per-gene statistics but would
   silently shrink a GSVA set, because GSVA de-duplicates set members.
   Additionally, **17 of the 18 arms are null-tested, not 18**: the
   `mtDNA-encoded OXPHOS` set is scored and reported on all four instruments but
   gets no percentile. All 13 of its genes sit in ventile 20 of 20 and 8 are
   among the 20 most abundant genes in the matrix (`MT-CO1` rank 1, `MT-ND4`
   rank 2); the median mtDNA gene is 11.5x the median gene of the ventile it
   would be matched against, so a matched draw substitutes genes an order of
   magnitude less abundant and the percentile would report the abundance gap.
   The mouse battery has no mtDNA arm either. A per-arm abundance-match guard
   now warns whenever an arm and its draws differ by more than 2x, so this class
   of mismatch is checked rather than noticed.
4. **Two descriptive instruments are computed alongside the two co-primary
   ones** - `content` (log2 sum of linear counts) and `zmean` (mean z of VST).
   They are the mouse's other two weightings and cost nothing. No claim rests on
   them and the "claim only what both support" rule still refers to GSVA and
   mitoPPS only.

## What script 07 does

Per the RESOLVED proposal and the amended plan:

1. **OXPHOS on BOTH instruments, co-primary.**
   - GSVA level on the VST matrix (`kcdf = "Gaussian"`) - the instrument that
     travels across cohorts, so Block F uses it.
   - mitoPPS on the LINEAR matrix - the instrument the mouse interaction was
     fitted on. TCGA-internal only; never compared across cohorts.
   - **Rule: report both, claim only what BOTH support.** One-instrument effects
     are instrument-dependent, not positive results.
2. **The specificity arms**, from Human MitoCarta 3.0 by pathway name, each
   answering a named alternative. `OXPHOS assembly factors` (68) is the PRIMARY
   negative. `Carnitine shuttle` (5) is merged into `Fatty acid oxidation` (44).
3. **2,000 expression-matched random gene sets** as the null for every arm.
4. **Per-complex sets** from **Human MitoCarta 3.0** - `CI subunits` 44,
   `CII` 4, `CIII` 10, `CIV` 21, `CV` 21 (37/4/9/18/19 nuclear). Corrected
   2026-08-28 after the first run: the curated substitute's `ATPase` class is
   `ATP13A1-ATP13A5`, P5-type cation ATPases with no ATP synthase subunit in it,
   and MitoCarta does carry a complex hierarchy after all. See the correction
   block in the specificity panel proposal section 7.
5. **Both D7 proliferation covariates**: `PROLIF_STD` (327 genes) and
   `PROLIF_DISJOINT` (318 - the 9 shared with stripped Felsher removed).
   `PROLIF_DISJOINT` is defined against M-a and **must never be used with M-b**.
6. mtDNA-encoded OXPHOS held separately, per standing convention.

Traps handled in the script, listed so a reader knows they were not missed:

- the two matrices never meet, and both assert their own `scale` field;
- symbols are rescued through MitoCarta's Sheet-3 synonyms **towards the matrix
  vocabulary** - the opposite direction from G1, which harmonised towards
  MitoCarta. `KARS` -> `KARS1` cost G1 a gene until it was caught;
- the map is built once over the union of all symbols and applied per
  collection, because several arm names are also MitoPathway names and the
  merged `Fatty acid oxidation` arm is NOT the `Fatty acid oxidation` pathway.
  Keying anything by set name across collections gives one the other's genes;
- every GSVA call carries two complementary half-matrix pin sets, so a call with
  20 pathways and a call with 2,000 random sets walk the same gene universe. The
  observed arm is re-scored inside its own null batch and the script stops if
  the value moves at all;
- the mitoPPS matrix rewrite is asserted against the definitional triple-loop
  form, and every arm that is a universe pathway must reproduce its canonical
  value exactly;
- the per-complex sets are checked against MitoCarta's nuclear OXPHOS subunits,
  which are an independent curation of the same genes. Note the source list is
  internally inconsistent about mtDNA - `Complex I` lists all seven `MT-ND`
  genes and `Complex III` lists `MT-CYB`, while `Complex IV` and `ATPase` list
  none - so the mtDNA strip is what makes the five sets comparable, not a
  formality.

## Two decisions RESOLVED 2026-08-28 - they no longer block 09

Both settled in `docs/2026-08-28_D8_D9_missing_data_and_plate.md`, from
structural counts only, with no outcome data read.

**D8 - missing data: complete cases, n = 938, no imputation.** The missingness
stacks - 84 of the 157 dropped are missing two covariates and 21 are missing
three - so no single covariate can be sacrificed to buy n (dropping purity
recovers 28, dropping TP53 recovers 0). Decisive reason: PAM50 is called from
expression and the exposures are scored from the same matrix, so imputing it
fills in the covariate that adjusts the exposure from the exposure's own data.
RNA is complete for all 1,095, so neither exposure nor endpoint is ever missing.
Script 03's proposed sensitivity was corrected - "only fully-observed covariates"
degenerates to plate alone - and is now a ladder: **M2** drops purity/PAM50/TP53
(n = 1,079), **M3** is plate only (n = 1,095).

**D9 - plate: fixed factor, levels of n < 10 pooled into `other`**, 41 -> 28
within the complete cases. TSS is not an alternative - 36 levels against 41, and
36 of the 41 plates span more than one TSS, so they are crossed, not nested. A
random intercept is the better model but is a sensitivity on the primary arm
only: the battery is 17 arms x 2,000 draws = 34,000 null fits per instrument, and
the null is not optional. One fixed pooling map across the observed model and
every draw; the same rule re-applied within a stratum after `droplevels()`.

## Smaller things still open

- **`PTEN_altered` counts only homozygous deletion**, giving 8.4%. Whether
  shallow loss (`-1`) should count is undecided. Not urgent.
- **Conditional enrichment for G2** - proposed in the G2 note section 6, not run.
  The Basal/TNBC enrichment there is partly definitional (the co-altered group is
  a subset of MYC-amplified, and MYC amp is itself basal-enriched). Needed
  before any Panel b figure.
- **I-SPY2 `mp` field** - roughly balanced 484/504, so probably MP1 vs MP2 within
  the high-risk criterion rather than high/low. Confirm before use (D5 note).
- **D6** - does the mouse arm have a metastasis phenotype to anchor F4 to.
  Untouched.
- **`M_a` vs `M_b` threshold** (rho >= 0.30) was proposed and passed at 0.613, so
  it is moot in practice but has not been formally signed off.

## State of the science, in one place

**Gates.** G1 fully discharged (count criterion + correlation criterion, the
latter passing at rho 0.249 vs a random-set 95th percentile of 0.013, 100.3% of
raw retained). G2 passed - `MCL1` +2 OR 1.69 (1.17-2.44), `BCL2L1` >=+1 OR 1.77
(1.26-2.47), `BBC3` loss FAILS and is indistinguishable from its `BAX` regional
control. G3 passed.

**Decisions.** D1-D5 and D7 resolved, each with a dated note. D6 open.

**The MYC axis is validated as an exposure** (`2026-08-28_myc_estimator_
validation.md`): M-a vs M-b rho 0.613; both ordered by MYC copy number
(Kruskal-Wallis chi-sq 187 and 131, p < 2.2e-16) on an instrument that shares no
gene with them; stripping cost nothing (0.997, 0.991); median M-a by PAM50 runs
Basal > Her2 > LumB > Normal > LumA, which nothing was fitted to produce.

**Two framing points that changed this session, from reading the mouse source:**

- The mouse OXPHOS decline is **age-related, not MYC-driven** - it falls on both
  timelines and the genotypes do not differ (p = 0.615). What is MYC-specific is
  that Foxo3 fails to rise and the priming ratio collapses. The coupling **flips
  sign** with genotype (WT -4.80, Myc+ +4.35), so the interaction is the right
  test and there is no main effect to add.
- The mouse interaction is **a lead, not a confirmed result** - its own scripts
  say so twice. It fails its permutation null (p = 0.083) and weakens with a
  timepoint term (0.0052 -> 0.088), at n = 6 per cell. And the specificity claim
  rests on **one comparator**. So the human arm is the adequately-powered first
  test, not a confirmation exercise. That is a stronger position, but it changes
  what a human null means and should be said in the text.

## Housekeeping

**`data/raw/` is 1.3 GB, gitignored, and NOT on origin. It still needs its own
backup.**

```
743M  tcga_pancanatlas      MC3 + ABSOLUTE + Thorsson + RPPA + arm calls
256M  tcga_brca_expression  42 GDC .tsv files + the counts cache
145M  gistic_tcga_brca      ISAR + Firehose
132M  tcga_expression_xena  the STAR counts matrix
```

Everything is re-downloadable from the URLs and SHA-256 sums in the `data/*/
README.md` files, but MC3 alone took ~50 minutes at 0.24 MB/s.

**Do not delete the 42 GDC `.tsv` files** under `data/raw/tcga_brca_expression/`.
Script 01 takes its gene annotation from one of them, and they are the only way
to re-run the Xena cross-check.

`gdc-client` 2.3.0 is installed at `/opt/homebrew/bin/gdc-client`. It is not in
Homebrew despite what a search suggests - it is a manual binary from NCI.
