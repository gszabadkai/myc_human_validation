---
date: 2026-08-29
status: disposable - delete once used
supersedes: 2026-08-28_handoff.md (spent; deleted in the same commit)
purpose: resume after Block C and Block B, at the F3 stop gate
---

# Handoff

Everything is committed and pushed. `main` is level with `origin`, tree clean.

**The pipeline through Block B is finished and run.** The next step is NOT code -
it is two data acquisitions that both turn out to be missing. See section 3.

## Resume prompt

Paste into a fresh session started in `/Users/gs/code/myc_human_validation`:

```
Read CLAUDE.md, then docs/2026-08-29_handoff.md.

Do NOT re-derive or re-audit anything. All gates and decisions are settled and
recorded in dated notes in docs/. The mouse repo is deliberately NOT attached;
read it read-only with
  git -C /Users/gs/G/data/MK_myc_2022/myc_mouse show HEAD:<path>

Scripts 00-10 are written AND RUN. H1 is falsified on both clauses, H2 and H3
are not supported, and the escape reading is not supported. Three of the four
falsification criteria are met; only H4 remains.

The F3 stop gate is blocked on two missing inputs (handoff section 3). Start by
telling me how you propose to resolve them, and what you would do in parallel.
Do not run scripts - I source them in Positron.
```

---

## 1. Where the work is

**Written and run:** `00`-`10`. **Not written:** `11`-`18`.

```
results/                                       880 MB total
  tcga_brca_vst.rds            LOG (VST)     -> GSVA
  tcga_brca_linear.rds         LINEAR        -> mitoPPS, and log2() for endpoints
  tcga_brca_genomics.rds       MC3, purity, leukocyte, aneuploidy, RPPA
  tcga_brca_covariates.rds     patient x covariate table + GISTIC
  tcga_brca_myc_scores.rds     M-a, M-b, M-c
  tcga_brca_mito_scores.rds    18 arms x 4 instruments + PROLIF
  mito_null/                   543 MB, 17 arms x 2,000 matched sets
  tcga_brca_priming.rds        PRIME, negatives, indices, FOXO3, 8 log2 limbs
  block_c_models.rds           116 coefficients + 68,000 null fits + paired null
  block_b_models.rds           E1-E4 + the E2 battery and its null
  g1_*.rds, g2_*.rds           the gates
```

n = 1,095 patients, 18,115 genes, **Block C/B analysis set 938**.

## 2. The science, in one place

**H1 is falsified on both clauses.** Clause 1 (Block C): the `MYC:OXPHOS`
interaction on PRIME is 0.020 (p 0.455) on GSVA and 0.010 (p 0.726) on mitoPPS,
and sits at percentile 31.6 of its expression-matched null - a random matched
gene set gives a *larger* coefficient. Clause 2 (Block B E1): no quadrant
enrichment for `BUFFER` on either instrument, in any of three forms, with the
instruments opposite in sign.

**H2 and H3 are not supported.** Every stratum flat; PI3K-intact is the flattest.

**The escape reading is not supported** and fails in the reversed direction - 34
of 36 E2 coefficients negative - but the matched null shows that reversal is not
mitochondrial (GSVA null median itself -0.043; OXPHOS at percentile 24.7).

**Three of four falsification criteria are met. Only H4 is untested.**

**What survives the whole arm - one finding, on both instruments:**

> `MYC x OXPHOS` associates with **lower BCL-XL and higher BIM**, PUMA unmoved.
> `BCL2` moves with `BCL2L1`, marginally; `MCL1` does not move. So the dangerous
> state carries more BH3-only signal and less guardian transcript, with **no
> compensating amplification**.

BIM is not a fishing result: `myc_mouse/scripts/44` sets
`PRESPEC <- c("Bbc3", "Bcl2l11")` from independent westerns. This arm's plan
mis-classified it as a negative control; corrected 2026-08-29 in the plan and in
the Block C note section 0.

**Two things reported and deliberately NOT pursued.** `Glycine metabolism` in
Block C, and `Folate and 1-C` in Block B (p_emp 0.002 / 0.001, both instruments).
Neither has a prior, unlike BIM. Chasing either is the fifth post-hoc hypothesis
plan section 2 forbids.

**One live commitment.** The BIM replication declared in the Block C note
section 9 - three models, directions fixed, failure condition stated, declared
before any replication cohort was opened. **Honour it or delete it; do not amend
it.**

## 3. THE TWO BLOCKERS - read before planning anything

Plan section 12 puts the **F3 incremental-value stop gate** next, before any
Block F code. F3 is

```
m0: Surv(...) ~ clinical      m1: m0 + MB1_forkscale      m2: m1 + STATE
```

Both of its inputs are absent from this repo:

**(a) No `MB1_forkscale` anywhere.** G3 is recorded as "passed" in one line of
the old handoff and there is **no G3 note in `docs/`**. Availability was checked;
the data was never snapshotted. Source per the plan:
`github.com/gszabadkai/Menegollo_Bentham`. Unresolved question: does forkscale
exist for METABRIC only, or can it be computed for TCGA? Block D (script 15)
assumes the former.

**(b) No survival data at all.** `data/tcga_clinical/` has 8 columns - PAM50,
ER/PR/HER2, aneuploidy, FGA - and no OS/DSS/DFI/PFI. `data/raw/tcga_pancanatlas/`
has MC3, ABSOLUTE, leukocyte, arm calls and RPPA, but **not** the Clinical Data
Resource. If any survival model is to run in TCGA it needs
`TCGA-CDR-SupplementalTableS1.xlsx` (Liu et al. 2018), snapshotted with a
provenance README like every other input.

**Also absent:** METABRIC, SCAN-B, and the three neoadjuvant GEO cohorts
(GSE194040 I-SPY2 primary, GSE164458 BrighTNess, GSE25066). F1, F2 and the BIM
replication all need them.

## 4. What I would do next, and why

**Recommended order, but this is a decision for the morning, not a settled one.**

1. **Fetch METABRIC** (expression + survival + treatment + clinical). One
   acquisition serves three pending jobs: the declared BIM replication (17), F2,
   and probably F3. Highest leverage by a distance.
2. **Resolve forkscale** in parallel - what the Menegollo repo actually exposes,
   and for which cohort. Without it F3 cannot run and Block F stays gated.
3. **Build Block G / DepMap (script 14) now, regardless.** Plan section 12 says
   it "can run in parallel from the start - it needs none of the TCGA work and it
   is the cheapest evidence in the whole plan". It is also **functional rather
   than observational**, which this arm now needs badly: it can ask whether
   `MYC x OXPHOS` predicts `BCL2L1` / `MCL1` CRISPR dependency, which speaks
   directly to the one finding that survived.
4. Then F3, and Block F only if F3 passes.

## 5. Decisions waiting

- **Does `STATE` get frozen as specified?** Script 11 freezes it. Levels 3 and 4
  differ only by `BUFFER`, and E1 found no excess buffering in that quadrant to
  condition on. **No outcome data has been seen**, so a pre-outcome revision is
  permissible. My recommendation is to **freeze it exactly as plan section 7.5
  specifies and say in the text that the level-3-vs-4 contrast has lost its
  motivation** - D5 already made the continuous three-way the primary, so STATE
  carries less than it did, and revising a pre-registered variable to suit a
  result we have seen is a worse trade than reporting the weakness.
- **Is the arm worth continuing at all?** Three of four falsification criteria
  are met. Plan section 15 describes the well-formed failure and it is available.
  Against that: H4 is untested, it is the counter-intuitive prediction the plan
  calls "worth a main panel", and Block C has now given it a mechanism (more BIM,
  less BCL-XL, no buffer -> predicted *more* chemosensitive). Worth deciding
  deliberately rather than by momentum.
- **D6** - does the mouse arm have a metastasis phenotype to anchor F4 to. Still
  open, untouched since 2026-08-27.

## 6. Housekeeping

- **`data/raw/` is 1.3 GB, gitignored, and still has no backup outside git.**
  Everything is re-downloadable from the URLs and SHA-256 sums in the
  `data/*/README.md` files, but MC3 alone took ~50 minutes.
- **`results/` is now 880 MB**, 543 MB of it `mito_null/`. Regenerable: script 07
  rebuilds it in ~35 minutes and is resumable per arm.
- Do not delete the 42 GDC `.tsv` files under `data/raw/tcga_brca_expression/` -
  script 01 takes its gene annotation from one of them.

## 7. The dated record, in reading order

```
2026-08-27_human_validation_plan.md          the plan, amended in place with dated blocks
2026-08-28_G1_result_and_decisions.md        G1, D2
2026-08-28_D4_scoping_and_G2_design.md       Lee et al., G2 design
2026-08-28_G2_result.md                      G2 passes; BBC3 fails
2026-08-28_D5_cohort_selection.md            neoadjuvant cohorts; H4 goes continuous
2026-08-28_D7_proliferation_covariate.md     S1/S2/S3
2026-08-28_specificity_panel_proposal.md     the battery (item 7 corrected)
2026-08-28_myc_estimator_validation.md       M-a/M-b/M-c
2026-08-28_D8_D9_missing_data_and_plate.md   complete cases; plate pooling
2026-08-29_script09_build_spec.md            the Block C model space, pre-fit
2026-08-29_block_c_result_H1_not_supported.md  H1 clause 1; BIM; the replication
2026-08-29_escape_reading_declaration.md     directions fixed before Block B
2026-08-29_block_b_result_escape_not_supported.md  H1 clause 2; the reversal
```
